function powerline_precmd() {
    POWERLINE_GO_SHELL_VAR="dcollins"
    POWERLINE_GO_MODULES="host,shell-var,ssh,cwd,perms,git,venv,exit,root"
    POWERLINE_GO_SHELL_VAR_BG="blue"
    POWERLINE_GO_SHELL_VAR_FG="white"
    POWERLINE_GO_GIT_BRANCH_BG="darkgreen"
    POWERLINE_GO_GIT_BRANCH_FG="white"
    PS1="$($GOPATH/bin/powerline-go -error $? -jobs ${${(%):%j}:-0} -modules host,shell-var,cwd,perms,git,venv,exit,root -shell-var POWERLINE_GO_SHELL_VAR -theme ~/.local/config/powerlinegotheme.json)"

    # Uncomment the following line to automatically clear errors after showing
    # them once. This not only clears the error for powerline-go, but also for
    # everything else you run in that shell. Don't enable this if you're not
    # sure this is what you want.

    #set "?"
}

function install_powerline_precmd() {
  for s in "${precmd_functions[@]}"; do
    if [ "$s" = "powerline_precmd" ]; then
      return
    fi
  done
  precmd_functions+=(powerline_precmd)
}

if [ "$TERM" != "linux" ] && [ -f "$GOPATH/bin/powerline-go" ]; then
    install_powerline_precmd
fi
